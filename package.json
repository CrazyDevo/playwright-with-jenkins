{
  "name": "playwright-for-jenkins",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "regression": "npx playwright test",
    "smoke": "npx playwright test --grep @smoke",
    "only": "npx playwright test --grep @only --headed --project=chromium",
    "smoke_allure": "npx playwright test --grep @smoke --reporter=line,allure-playwright"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.43.1",
    "@types/node": "^20.12.8",
    "allure-playwright": "^2.15.1"
  }
}
